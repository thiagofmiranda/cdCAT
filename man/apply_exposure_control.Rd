% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exposure_control.R
\name{apply_exposure_control}
\alias{apply_exposure_control}
\title{Apply exposure control to item selection}
\usage{
apply_exposure_control(scores, available, exposure, n_administered)
}
\arguments{
\item{scores}{Numeric vector of criterion scores for the candidate
items (same length and order as \code{available}).}

\item{available}{Integer vector of candidate item indices (items
eligible for selection at this step).}

\item{exposure}{Numeric vector of length J (total items) with
exposure parameters, or \code{NULL} for no control.  See Details.}

\item{n_administered}{Integer. Number of items already administered
(used to determine position \eqn{k} in the Randomesque regime).}
}
\value{
Integer scalar -- global index of the selected item.
}
\description{
Selects the next item from the candidate pool according to the
exposure control regime encoded in \code{exposure}.
}
\details{
Two regimes are supported and detected automatically from the values
of \code{exposure}:

\strong{Sympson-Hetter} (Sympson & Hetter, 1985) -- triggered when \strong{all}
values in \code{exposure} are in \eqn{[0, 1]}.  Each item has an
acceptance probability equal to its \code{exposure} value.  Items are
visited in descending score order; the first one that passes the
random draw is selected.  Values near 1 are almost always selected;
values near 0 are rarely selected.

\strong{Randomesque} -- triggered when \strong{all} values in \code{exposure} are
\eqn{\geq 1}.  At position \eqn{k} in the test (i.e. when selecting
the \eqn{k}-th item), the top \code{exposure[k]} candidates by score are
collected and one is drawn at random.  \code{exposure[k] = 1} is
equivalent to greedy selection.

When \code{exposure} is \code{NULL}, greedy selection (\code{which.max(scores)}) is
used, equivalent to no exposure control.
}
\examples{
scores    <- c(0.8, 0.5, 0.3, 0.9)
available <- c(2L, 4L, 6L, 8L)

# Greedy (no exposure control)
apply_exposure_control(scores, available, exposure = NULL, n_administered = 0L)

# Sympson-Hetter: item 8 (score 0.9) has probability 0.2 of being accepted
set.seed(1)
exposure_sh <- rep(0.8, 10)
exposure_sh[8] <- 0.2
apply_exposure_control(scores, available, exposure_sh, n_administered = 2L)

# Randomesque: at position 3, draw from top-2 candidates
exposure_rq <- rep(1, 10)
exposure_rq[3] <- 2
set.seed(1)
apply_exposure_control(scores, available, exposure_rq, n_administered = 2L)
}
\references{
Sympson, J. B., & Hetter, R. D. (1985). \emph{Controlling item-exposure
rates in computerized adaptive testing}. Proceedings of the 27th
annual meeting of the Military Testing Association (pp. 973--977).
}
\seealso{
\code{\link[=apply_sympson_hetter]{apply_sympson_hetter()}}, \code{\link[=apply_randomesque]{apply_randomesque()}},
\code{\link[=select_next_item]{select_next_item()}}, \link{CdcatSession}
}
