% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shadow_cat.R
\name{.apply_shadow_cat}
\alias{.apply_shadow_cat}
\title{Apply shadow CAT item selection (internal)}
\usage{
.apply_shadow_cat(items, est, administered, criterion, constr_fun)
}
\arguments{
\item{items}{A \code{cdcat_items} object.}

\item{est}{A \code{cdcat_est} object returned by \code{\link[=estimate_alpha]{estimate_alpha()}}.}

\item{administered}{Integer vector of already administered item
indices.}

\item{criterion}{Upper-cased criterion string.}

\item{constr_fun}{A function with signature
\verb{function(scores, items, administered)} that returns a single
integer — the global index of the next item to administer.}
}
\value{
Integer scalar — global index of the selected item.
}
\description{
In shadow mode the full item bank is considered at every step.
Criterion scores are computed for \strong{all J items} (including already
administered ones) and then handed to the user-supplied
\code{constr_fun}, which is responsible for enforcing any linear or
combinatorial constraints and returning the index of the next item.
}
\details{
This design keeps \code{cdCAT} free of LP-solver dependencies: the user
can call any solver they wish (e.g. \code{lpSolve::lp()},
\code{ROI::ROI_solve()}) inside their \code{constr_fun}.
}
\keyword{internal}
