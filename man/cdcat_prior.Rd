% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prior.R
\name{cdcat_prior}
\alias{cdcat_prior}
\title{Build a named prior distribution over skill profiles}
\usage{
cdcat_prior(..., K = NULL, normalize = TRUE)
}
\arguments{
\item{...}{Named numeric values. Each name must be a binary string of
length K (e.g. \code{"00"}, \code{"01"}, \code{"10"}, \code{"11"} for K = 2).
All 2^K patterns must be supplied.}

\item{K}{Integer. Number of attributes. Inferred from pattern length
if not supplied.}

\item{normalize}{Logical. If \code{TRUE} (default), the vector is
automatically normalized to sum to 1 with a warning when needed.}
}
\value{
A named numeric vector of length 2^K whose entries sum to 1,
ordered consistently with \code{\link[=build_skill_patterns]{build_skill_patterns()}}.
}
\description{
Constructs and validates a prior probability vector over all 2^K
latent skill profiles, using pattern names as keys.
The pattern names follow the same ordering as \code{\link[=build_skill_patterns]{build_skill_patterns()}}:
\code{expand.grid} with the first attribute varying fastest.
}
\examples{
# K = 2: four profiles — "00", "10", "01", "11"
prior <- cdcat_prior("00" = 0.4, "10" = 0.2, "01" = 0.2, "11" = 0.2)

# Use in a session
Q <- matrix(c(1,0, 0,1, 1,1), nrow = 3, ncol = 2, byrow = TRUE)
items <- cdcat_items(Q, "DINA", slip = c(0.1,0.1,0.1), guess = c(0.2,0.2,0.2))
session <- CdcatSession$new(items, prior = prior)
}
\seealso{
\code{\link[=estimate_alpha]{estimate_alpha()}}, \link{CdcatSession}
}
